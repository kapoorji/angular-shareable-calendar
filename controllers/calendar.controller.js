angular.module('app').controller('CalendarCtrl', function($scope, $filter, $rootScope) {
  
});
/* angular.module("app").directive("clDatesLink", function($compile) {
    return {
        restrict: "A",
        link: function(scope,element,attrs){
			element.ready(function () {
				if(element.find('a').length > 0){
					element.removeClass('calignmiddle').addClass('caligntop');
				}
			});

        }
    };
}); */

